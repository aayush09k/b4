
class P2PConnection {

    static P2PConnection createConnection(EndPointAddress dest)
    // returns null if connection setup fails, else connection object is returned.
    // dest is webRTC offer which can be used by any webRTC client to setup a connection through STUN or TURN server.
    {
    
    }
    
    sendMessage( Message)
    {
    
    }
    
    Message receiveMessage()
    {
    
    }
    
    Bool getStatus()
    {
    
    }

}
